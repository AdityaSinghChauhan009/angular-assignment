import { ChangecolourDirective } from './changecolour.directive';

describe('ChangecolourDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangecolourDirective();
    expect(directive).toBeTruthy();
  });
});
